version: '3.8'
services:
  server:
    build: .
    ports:
      - '5000:5000'
    environment:
      - PORT=5000
      - MONGO_URI=mongodb://mongo:27017/av-hall-booking
    depends_on:
      - mongo
  mongo:
    image: mongo:6
    ports:
      - '27017:27017'
